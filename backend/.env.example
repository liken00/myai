# PoYo.ai API Key（必需）
# 从 https://poyo.ai/dashboard 获取
POYO_API_KEY=your_poyo_api_key_here

# Flask 环境（可选）
# 开发环境设为 development，生产环境设为 production
FLASK_ENV=production

# 服务器监听端口（可选，默认 5000）
PORT=5000

# CORS 许可的前端 URL（可选，* 表示允许所有）
# 示例：https://liken00.github.io
FRONTEND_URL=*
